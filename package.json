{
  "name": "@conectores_cielo/cielo-applepay-headless-vtex",
  "version": "1.0.0",
  "description": "Apple Pay headless orchestration packages for VTEX + Cielo eWallet checkouts",
  "packageManager": "yarn@1.22.19",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "yarn workspace @conectores_cielo/cielo-applepay-headless-vtex-core build && yarn workspace @conectores_cielo/cielo-applepay-headless-vtex-adapter build && yarn workspace @conectores_cielo/cielo-applepay-headless-vtex-react build",
    "check:bom": "node scripts/check-bom.js",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage.enabled true --coverage.provider v8 --coverage.reporter=text --coverage.reporter=lcov --coverage.reporter=json-summary",
    "postinstall": "node fix-bom.js"
  },
  "keywords": [
    "apple-pay",
    "cielo",
    "vtex",
    "payments",
    "headless-commerce"
  ],
  "author": "Cielo",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/e2m-store/cielo-applepay-headless-vtex.git"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^2.1.8",
    "tsup": "^8.3.5",
    "typescript": "^6.0.3",
    "vitest": "^2.1.8"
  }
}
